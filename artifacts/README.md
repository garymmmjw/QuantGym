# QuantGym Artifacts

This folder is for local QA outputs, screenshots, OCR pages, and one-off extraction artifacts. These files are not runtime dependencies and are ignored by Git by default.

Use this directory freely during testing, but promote only durable source code, normalized data, or runtime assets into tracked paths.
