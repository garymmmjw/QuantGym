# Product Status

This file keeps product state and roadmap notes out of the root README.

## Current Beta

- Private beta deployment targets Cloudflare Pages for the web app and Render for the API and LLM proxy.
- Closed beta access is controlled by `QUANTGYM_BETA_EMAIL_ALLOWLIST` on the API service.
- Render LLM deploy compatibility is covered by `npm run check:render-llm-deploy`; post-incident hosted status plus failure-email audit is recorded by `npm run check:render-llm-deploy-status` in `docs/browser-audit-screenshots/357-render-llm-deploy-status-summary.json`.
- Email verification uses SMTP, currently documented for Resend.
- The frontend build writes deployment endpoints and build provenance into
  `dist/config.js`, mirrored by `dist/version.json`.

## Completed Areas

- Cream / lavender / blue UI system with sidebar, top search, account chips, streak chip, score chip, Quant Wire, and generated mascot assets.
- Dashboard, prep plan, mock interview, interview experience notes, problem bank, skills radar, resume, jobs, community, network, mental math, news, and resources modules.
- Problem bank with 15 active sources and 2,997 compiled catalog problems.
- Personal favorites, problem social likes/comments, hot ranking, last score tracking, and mock-interview reuse.
- Email registration/login/password reset, Google JWT/JWKS login verification, cloud sync, localStorage fallback, and SQLite-backed state.
- LLM proxy for mock interviews, short feedback, study-log classification, resume advice, and RSS-based news.
- LeetCode Hot 100 tracking panel and packageable Chrome collector extension.

## Known Limits

- The frontend is still a static Vite app. Static route integrity, route-interaction contracts, formal module ownership with every browser-smoke interaction mapped to an owner, a real Chrome browser route smoke, UI contracts, and browser-evidence manifests are covered by local gates; beta usage should keep adding browser-executed journeys around newly identified high-risk paths.
- SQLite is fine for private beta and small groups. A read-only preflight/export script, API `/api/health` database-backend disclosure, `api-server/postgres/schema.sql`, `npm run check:postgres-cutover`, `npm run check:postgres-cutover:export-smoke`, `npm run build:postgres-cutover-packet`, and `npm run check:postgres-cutover:complete` now cover backup, redacted/full export safety, rejection of row-limited/truncated migration exports, schema/data shape checks, an offline include-sensitive export import plan, migration-window handoff files, and final cutover evidence positive/negative signoff shape including pending status, localhost, loopback, private-network, raw-IP/malformed target-host/database, database DSN, placeholder/private/raw-IP/credential/query evidence URLs, unsafe runtime health URLs, future timestamp, source/export SHA mismatch, target row-count mismatch, inactive app database, non-Postgres runtime backend evidence, missing backup, and runtime database health visibility, but a public multi-user release should still complete a real Postgres migration and deployment.
- Basic admin metrics/audit endpoints, an account-page ops overview, 24h HTTP error observability, optional HTTP error webhook alerts, API auth rate-limit enforcement, trusted-proxy CIDR controls for forwarded client IP headers, `npm run check:ops-alerts:runtime-smoke`, `npm run check:ops-alerts:production-fixture`, `npm run build:ops-alert-edge-packet`, and `npm run check:ops-alerts:production` readiness validation are available. The runtime smoke now verifies 404, auth 401, auth 429, spoofed `X-Forwarded-For` Google-login rate-limit behavior, alert delivery, HMAC webhook signatures, and external alert payload sanitization; the production fixture verifies the production signoff gate rejects placeholder/short webhook tokens, placeholder/local/private-network/raw-IP/credential-bearing/query-bearing/incomplete alert endpoints, wildcard proxy trust, generic edge notes that do not identify `/api/auth/*`, client IP/identity, and an enforcement action, plus unsafe edge-rate-limit settings/evidence URLs without leaking raw tokens or full dashboard URLs. The webhook-smoke path now signs the raw body, rejects generic 2xx receivers that do not acknowledge signature verification, and fails before external delivery whenever production configuration checks fail, so an unsafe signoff attempt cannot send a readiness payload to the configured receiver. The ops packet generates the Render env template, signed webhook contract, Cloudflare `/api/auth/*` edge-rule runbook, smoke payload, and signoff checklist, and its final signoff requires both `npm run check:ops-alerts:production` and `npm run check:ops-alerts:production -- --smoke`. Production still needs a real alert receiver that accepts, verifies, and acknowledges the smoke payload plus edge-level rate limits configured with provider, notes, and HTTPS evidence URL signoff.
- The beta web app is live on `beta.quantgym.app`, but `quantgym.app` and `www.quantgym.app` are not yet usable HTTPS promotion domains; recent checks timed out, and earlier checks observed Cloudflare 525 SSL handshake errors. `npm run check:apex-www-domain` records the live DNS/HTTPS diagnosis, owner action, remediation checklist, and acceptance criteria in `docs/browser-audit-screenshots/355-apex-www-domain-summary.json`, and `npm run check:apex-www-domain -- --require-clear` is the clear-signoff command once Cloudflare/origin SSL or an intentional redirect is fixed. Keep the apex/WWW Cloudflare or origin SSL routing/redirect setup as a separate domain follow-up.
- Community image/video posts, account avatars, Memory resource images, and interview answer attachments now prefer the authenticated `/api/media` upload path and store returned media URLs in state JSON. Media storage supports local disk and S3/R2-compatible buckets; `npm run check:media-storage:runtime-smoke` covers local authenticated upload/download, persistence, audit logging, failure codes, MIME-derived object extensions when filenames disagree with payload type, direct-client spoofed forwarded host/proto headers being ignored for returned media URLs, and the S3/R2-compatible signed PUT/read-through/public-URL behavior against a temporary fake object store. `npm run check:media-storage:production-fixture` verifies the production gate rejects local/private-network/raw-IP object storage and public media URL shapes, credential-bearing or query/fragment-bearing endpoint/public-base URLs, raw provider object-storage public hosts, placeholder/short object-storage credentials, unsafe bucket names/prefixes, and live-smoke cleanup, public Content-Type, and public Range GET behavior against isolated fixtures; `npm run build:media-storage-packet` generates the Render env template, R2/S3 bucket/CDN runbook, object-storage contract, and live-smoke checklist; `npm run check:media-storage:production` validates production env shape; and `npm run check:media-storage:production -- --live` can sign off a real bucket/CDN by writing, reading, publicly fetching with Content-Type preserved, verifying `Range` returns `206` plus `Content-Range`, and deleting a tiny readiness object. The final media signoff requires both commands. Production still needs a real bucket/CDN configured. LLM interview requests may still carry transient attachment contents for evaluation.
- The jobs module has a read-only `/api/jobs` catalog endpoint, static fallback data, optional crawler/vendor JSON feed ingestion, `npm run check:jobs-source:runtime-smoke` coverage for local feed ingest/cache/fallback behavior including unsafe source URL sanitization, unknown source type defaulting, invalid/future `postedAt` sanitization, HTTP errors, invalid JSON, and oversized payloads, `npm run check:jobs-source:production-fixture` coverage for production-shape, default public-ATS feed behavior, private-network/raw-IP/credential-bearing/query-bearing source URL rejection, placeholder/short source-token rejection, and live-feed data-quality guardrails, `npm run build:jobs-feed:public-ats`, `npm run check:jobs-feed:static`, and `npm run build:jobs-feed:publication-packet` for a checked-in Greenhouse source list, generated public-ATS snapshot at `public/data/jobs/public-ats-feed.json`, beta/static URL readiness for `https://beta.quantgym.app/data/jobs/public-ats-feed.json`, SHA-256 evidence, source list, stable HTTPS DNS-hostname hosting runbook, production env template, and live-signoff checklist, front-end safe timestamp sorting for `crawler-ready` fallback jobs covered by route contracts plus browser smoke, and `npm run check:jobs-source:production -- --live` readiness validation for a real feed with internship and fulltime roles. Production-like API deployments default to the deployed static feed when `QUANTGYM_JOBS_SOURCE_URL` is unset; as of 2026-06-19, `npm run check:jobs-api:deployed-source` passes against `https://api.quantgym.app/api/jobs` with `catalog+source`, 160 source jobs, 6 fallback catalog jobs, both role types, and real metadata. Keep the public-ATS refresh cadence monitored and rerun the deployed source smoke after feed refreshes or API deploys.
- The Chrome extension has Manifest V3 validation, popup runtime smoke coverage, remote Board URL HTTPS enforcement with loopback HTTP allowed for local development, store-readiness validation, store listing assets, a public privacy page, a packaging script with zip/per-file SHA-256 integrity evidence, `npm run check:chrome-store-publication` for upload handoff, `npm run check:chrome-store-publication:fixture` for the published-signoff evidence contract including placeholder-looking item-id rejection, canonical Chrome Web Store `/detail/<extension-slug>/<item id>` detail URL enforcement, evidence URL binding to the same Chrome Web Store item id, private-network/raw-IP listing and evidence URL rejection, credential/query URL rejection, submitted-version matching against the manifest, and upload SHA-256 matching against the current release package, and `npm run build:chrome-store-publication-packet` for the developer-dashboard submission packet, listing snapshot, package SHA-256 evidence, published signoff env template, and final checklist. Chrome Web Store submission and review are still separate developer-account steps until `npm run check:chrome-store-publication:published` passes with the real published item evidence.
- Question-bank source rights now have a machine-readable `source-rights-manifest.json`, `npm run check:question-bank-rights` for the current private beta, `npm run check:question-bank-rights:public-smoke` for the public/commercial approval schema, `npm run check:question-bank-rights:release-blockers` to prove the current public/commercial gates remain blocked for all active needs-review sources, and `npm run build:question-bank-rights-packet` to generate the per-source approval packet. The packet summary in `docs/browser-audit-screenshots/345-question-bank-rights-packet-summary.json` records the final public/commercial signoff command, release-blocker refresh command, 15 source packets, TODO-only manifest drafts, required commercial scopes, and evidence URL safety rules; `docs/browser-audit-screenshots/341-external-launch-blockers-summary.json` surfaces the same packet coverage in the launch-blocker rollup. The public smoke fixture proves valid public/commercial approvals can pass while public-only approval is rejected for commercial mode, and it rejects placeholder/private-network/raw-IP/credential-bearing/query-bearing evidence URLs, stale reviews, missing direct-permission grantors, and unsupported redistribution scopes. Public/commercial distribution still requires per-source approval with recent review dates, non-placeholder externally reachable HTTPS evidence URLs with DNS hostnames and without embedded credentials/query/fragment parts, approval types, evidence summaries, and redistribution scopes, and `npm run check:question-bank-rights:public` plus `npm run check:question-bank-rights:commercial` must pass before release.

## Outstanding Items

Run `npm run check:apex-www-domain` first to refresh the live apex/WWW DNS/HTTPS diagnosis in `docs/browser-audit-screenshots/355-apex-www-domain-summary.json`, then run `npm run check:external-launch-blockers` to generate the machine-readable blocker summary in `docs/browser-audit-screenshots/341-external-launch-blockers-summary.json`; it now also surfaces the deployed beta smoke evidence from `docs/browser-audit-screenshots/351-deployed-beta-smoke-summary.json` and the isolated mobile content evidence from `docs/browser-audit-screenshots/352-deployed-beta-mobile-content-smoke-summary.json`. `npm run check:release-readiness:local` refreshes the apex/WWW diagnosis before the same blocker gate without overwriting the standalone `341` summary. Local release-readiness now auto-starts and tears down the loopback API and LLM proxy for the Production boundaries gate when `config.js` points at `127.0.0.1`; without a fresh `QUANTGYM_GOOGLE_ID_TOKEN`, that gate is expected to remain a token-only partial. Deployed Google provider login evidence is short-lived and only clears the external blocker while the recorded `tokenExpiresAt` remains at least 120 seconds in the future; once it expires, `npm run check:external-launch-blockers` marks the item blocked again. Rerun `npm run google:token-helper:deployed` and `npm run verify:production-boundaries:deployed:paste-token` when refreshing that evidence with the deployed Client ID. The deployed helper is an external-browser Console handoff for `https://beta.quantgym.app`, not a loopback Google sign-in page, so it avoids Google `origin_mismatch` for the deployed OAuth Client ID. After main deploys, run `npm run check:deployed-beta-smoke -- --password-stdin` with a beta account to refresh `351` and verify real hosted login, production API deploy-readiness wait, production API CORS preflights for sync and Poker room join, missing hashed `/assets/*.js` paths returning 404/no-store instead of `200 text/html`, plus all 21 deployed routes, then run `npm run check:deployed-beta-mobile-content-smoke` to refresh `352` and verify the hosted beta bundle can run mobile Experiences save/filter/share plus mobile News submit/filter/detail/read reload persistence without mutating a real beta account. Use `npm run check:external-launch-blockers -- --require-clear` only when preparing to clear every external public-launch blocker.

Operational optimization to complete in Render: configure the `quantgym-api` service build filter so docs, frontend-only, and tooling-only commits do not auto-deploy and briefly restart the API. The deployed beta smoke now waits for API health and CORS readiness, and `npm run check:deployed-beta-smoke:deploy-window-fixture` covers the transient `502`/missing-CORS deploy window locally, but reducing the real short interruption requires Render service configuration. The local handoff now has `npm run check:render-api-build-filter:fixture`, `npm run build:render-api-build-filter-packet`, and `npm run check:render-api-build-filter:production`; the production signoff requires `api-server/**` and `data/**` exactly, accepts dashboard, CLI, API, or complete Blueprint methods, rejects docs/frontend/tooling paths, requires specific notes plus an HTTPS Render evidence URL, and avoids writing a default production evidence file when the real signoff environment is missing.

1. Fix the Cloudflare/origin SSL handshake, timeout, or routing issue, or intentionally redirect the apex and WWW domains (`quantgym.app` and `www.quantgym.app`) so they return usable HTTPS 2xx/3xx responses, then make `npm run check:apex-www-domain -- --require-clear` pass. This is a tracked outstanding item, deferred by the 2026-06-17 handoff and reaffirmed on 2026-06-18 because `beta.quantgym.app` is the current beta entrypoint; the 2026-06-18 user decision is to leave it in the unresolved backlog for now. Do not block the beta on this, but resolve it before formally promoting the apex/WWW domains.
2. Configure a real external alert receiver and confirm edge-level rate limits in Cloudflare, Render, or the reverse proxy, then make `npm run check:ops-alerts:production` and `npm run check:ops-alerts:production -- --smoke` pass against real production variables including a non-placeholder `QUANTGYM_ALERT_WEBHOOK_TOKEN` of at least 24 characters, `QUANTGYM_EDGE_RATE_LIMIT_PROVIDER`, specific `QUANTGYM_EDGE_RATE_LIMIT_NOTES`, and `QUANTGYM_EDGE_RATE_LIMIT_EVIDENCE_URL`. The edge notes must identify the protected auth surface such as `/api/auth/*`, the client identity/IP characteristic, and the enforcement action such as block/challenge/throttle/rate-limit. The production gate rejects localhost, loopback, private-network, raw-IP, credential-bearing, and query/fragment-bearing webhook/evidence URLs so this signoff must point at externally reachable HTTPS DNS-hostname surfaces without URL-embedded secrets, and the smoke gate must receive a 2xx response plus `X-QuantGym-Alert-Verified: 1` or JSON `verified: true` from the real alert receiver for the signed `ops.readiness.smoke` payload only after the production config checks pass. The local API-to-webhook, auth failure alert sanitization, API auth rate-limit runtime paths, production-signoff fixture coverage, webhook-smoke payload safety/signing/verification acknowledgement, no-delivery behavior when production config is invalid, and deployment handoff packet are covered by `npm run check:ops-alerts:runtime-smoke`, `npm run check:ops-alerts:production-fixture`, and `npm run build:ops-alert-edge-packet`.
3. Configure a real S3/R2 media bucket plus CDN/public base URL for production uploads, then make `npm run check:media-storage:production` and `npm run check:media-storage:production -- --live` pass against those variables. The production gate now rejects localhost, loopback, private-network, raw-IP, credential-bearing, query/fragment-bearing, or raw provider object-storage endpoint/public media base URLs, and also rejects placeholder/short object-storage credentials plus unsafe bucket names or object prefixes, so this signoff must use externally reachable HTTPS DNS-hostname storage/CDN origins with real provider credentials and no URL-embedded secrets. The live gate must write, signed-read, public-read with Content-Type preserved, public Range-read with `206` plus `Content-Range`, and delete the readiness object, including cleanup on failed public reads. The local API media upload/download, MIME-derived object-extension handling, spoofed forwarded host/proto rejection for returned media URLs, S3/R2-compatible runtime paths, production/live fixture guardrails, and deployment handoff packet are covered by `npm run check:media-storage:runtime-smoke`, `npm run check:media-storage:production-fixture`, and `npm run build:media-storage-packet`.
4. Maintain the deployed production API default public-ATS jobs source and rerun `npm run check:jobs-api:deployed-source` after feed refreshes or API deploys to prove `/api/jobs` still returns the source-backed feed with both role types, unique ids, valid HTTP(S) URLs, and real company/title/postedAt fields with valid, non-future dates. Production-like API deployments default to `https://beta.quantgym.app/data/jobs/public-ats-feed.json` when `QUANTGYM_JOBS_SOURCE_URL` is unset; set `QUANTGYM_JOBS_SOURCE_URL` only to override it with another real internship/full-time crawler/vendor feed. The production gate rejects localhost, loopback, private-network, raw-IP, credential-bearing, and query/fragment-bearing explicit jobs source URLs, and rejects placeholder or short source tokens when configured, so overrides must use externally reachable HTTPS DNS-hostname feeds plus safe authentication when authentication is needed. The local feed ingest/cache/fallback path, including unsafe URL sanitization, unknown type defaulting, invalid/future `postedAt` sanitization, invalid JSON, oversized payload fallback, production-shape rejection, live-feed quality fixtures, the checked-in public-ATS static feed, and public-ATS hosting packet are covered by `npm run check:jobs-source:runtime-smoke`, `npm run check:jobs-source:production-fixture`, `npm run check:jobs-feed:static`, `npm run check:jobs-source:production -- --live`, and `npm run build:jobs-feed:publication-packet`.
5. Configure the Render `quantgym-api` service build filter with included paths exactly `api-server/**` and `data/**`, then make `npm run check:render-api-build-filter:production` pass with method, service name, specific notes, and a safe HTTPS Render evidence URL. `npm run check:render-api-build-filter:fixture` proves the signoff accepts dashboard and CLI methods for the valid path set while rejecting missing `data/**`, `docs/**`, `src/**`, unsafe evidence URLs, generic notes, and missing-env production probes that would otherwise leave a default 360 failure summary; `npm run build:render-api-build-filter-packet` generates the dashboard checklist, Render CLI command, reference Blueprint/API payload, signoff env template, and checklist. Do not include docs/frontend/tooling paths or commit a partial `render.yaml` unless the full Render service config is represented.
6. Submit and publish the Chrome Collector through the Chrome Web Store from the developer account after `npm run check:browser-extension:runtime-smoke`, `npm run check:chrome-store-readiness`, `npm run check:chrome-store-publication`, `npm run check:chrome-store-publication:fixture`, and `npm run build:chrome-store-publication-packet` pass against the hashed release package, store listing snapshot, dashboard handoff packet, and signoff contract. After publication, make `npm run check:chrome-store-publication:published` pass with the real non-placeholder item id, canonical Chrome Web Store `/detail/<extension-slug>/<item id>` detail listing URL ending in that item id, matching Chrome Web Store evidence URL for the same item id, published status, submitted version matching the manifest, and upload SHA-256 matching the current release package; the signoff gate rejects localhost, loopback, private-network, raw-IP, non-store, wrong-item, credential-bearing, query/fragment-bearing, child-path, and extra-segment listing/evidence URLs, so the listing/evidence URLs must use HTTPS DNS hostnames.
7. Execute the public-release database cutover from SQLite to Postgres using `api-server/postgres/schema.sql` and a protected, non-truncated `--include-sensitive` SQLite export as migration input, then make `npm run check:postgres-cutover:complete -- --db "$QUANTGYM_DB" --export /secure/quantgym-sqlite-export.json` pass with real managed Postgres DNS target host/database, timestamp, HTTPS DNS-hostname evidence URL, source/export SHA-256, target row count, app DB activation, observed `/api/health` runtime backend `postgres`, runtime health URL, and backup confirmation. Local schema/export/import-plan/signoff-shape safety, including complete-signoff positive and negative fixtures plus the migration-window packet, is covered by `npm run check:postgres-cutover`, `npm run check:postgres-cutover:export-smoke`, and `npm run build:postgres-cutover-packet`; the complete signoff rejects pending status, localhost, loopback, private-network, `.local`, raw-IP/malformed target-host/database, database DSN values, placeholder/private/raw-IP/credential/query evidence URL values, unsafe runtime health URLs, future timestamps, source/export SHA mismatches, target row-count mismatches, inactive app DB confirmation, non-Postgres runtime backend evidence, and missing backup confirmation.
8. Reconfirm each active problem-source rights boundary before any public or commercial distribution, record approvals with valid `reviewedAt` dates, non-placeholder externally reachable HTTPS `evidenceUrl` values with DNS hostnames and without embedded credentials/query/fragment parts, `approvalType`, `redistributionScope`, and `evidenceSummary` in `data/question-banks/source-rights-manifest.json`, then make `npm run check:question-bank-rights:public` and `npm run check:question-bank-rights:commercial` pass. `npm run check:question-bank-rights:public-smoke` verifies the approval schema locally with positive and negative fixtures, including public-only approval rejected for commercial mode, stale approval, missing grantor, unsupported scope, and placeholder/private-network/raw-IP/credential/query evidence URL rejection; `npm run check:question-bank-rights:release-blockers` verifies the current real catalog remains blocked until those approvals exist; and `npm run build:question-bank-rights-packet` keeps the per-source approval packet, final signoff command, release-blocker refresh command, TODO-only manifest draft, and evidence URL safety rules current in `docs/browser-audit-screenshots/345-question-bank-rights-packet-summary.json`.
9. Continue expanding browser-executed frontend interaction coverage as beta usage reveals new high-risk paths. Formal module ownership is now covered by `npm run check:module-ownership`, which maps all 21 route modules to owner groups, page/feature files, state domains, and all 67 current browser-route-smoke interactions. `npm run check:browser-route-smoke` covers authenticated rendering for all 21 routes, logged-out protected-route redirect plus local email registration/logout/password-step/relogin/password-reset behavior, and key Overview CTA navigation, Overview leaderboard controls/news ticker navigation, Streak check-in/calendar persistence, Shell sidebar collapse/reload persistence plus top command chat/account/settings shortcut navigation, legacy hash deep-link redirects with query preservation, mobile Shell sidebar/search/settings compact controls with no horizontal overflow, mobile module navigation menus from Training to Problems and Resources to Library with no horizontal overflow, Skills radar/global-search spotlight, Global search module/problem/job/company/course/news navigation, global search keyboard navigation through focused results, Problems detail/reveal/save, Problems pagination/collection filter/mock-interview handoff, mobile Problems detail action buttons/answer reveal/save/mock-interview handoff with no horizontal overflow, Problems ranking detail navigation, social no-cloud guard behavior, and LeetCode Hot 100 tracking persistence, Tools drill, Tools Mental Math full-session record persistence, Tools market game quote validation/scoring/persistence, Poker default route local/no-auto-join guard, Poker demo table start/action/persisted room state, Poker preflop matrix position, hand selection, and leave-table navigation, PK match/submit/reveal/record persistence, Plan create/edit/task persistence/navigation, Plan baseline diagnostic completion/reload persistence, mobile Interview advanced type/category/source controls with no horizontal overflow, Interview onboarding/practice answer/favorite/exit/resume, Interview attachment upload preview/transcript/request payload, Interview PDF source upload/question generation/session start, Todo add behavior, Todo edit/complete/delete/reload persistence, Community post persistence, Community image post fallback/reload persistence, Community video post fallback/reload persistence, Community direct message from post into Messages thread persistence, mobile Community posting, like/comment, direct-message navigation, and Messages reply/reload persistence with no horizontal overflow, Messages thread persistence, Messages multi-thread unread badge/read-state persistence, Experiences create/edit/share/delete persistence, News manual submit/filter/detail/read/reload persistence, mobile Experiences save/filter/share and mobile News submit/filter/detail/read reload persistence with no horizontal overflow, Memory resource/source persistence, Memory image upload fallback/reload persistence, Network contact persistence, Resume text persistence, Resume LLM review request/render/reload persistence, mobile Resume review controls with no horizontal overflow, Jobs filter/apply-link behavior, Companies tier/practice/careers-link behavior, mobile Jobs/Companies filtering, external link safety, and practice navigation with no horizontal overflow, Library search/kind filter/practice navigation/reader-guard behavior, Library cloud PDF reader open/link/close behavior, a cross-module Library -> Problems -> Todo -> Resume -> Settings persistence journey, Courses path/source/note persistence, Account profile persistence, Account local email change password guard/relogin persistence, Account avatar upload/clear plus resume file persistence, Account non-admin cloud sessions avoiding admin endpoint requests, mobile Account profile/upload/security controls with no horizontal overflow, Settings language switch URL sync/reload persistence, Settings runtime config persistence plus Google Client ID clear/reload behavior, Settings Sync Cloud no-session guard behavior without `/api/sync` calls, Settings Sync Cloud success behavior with state/community/account payload and status persistence, mobile Settings config/backup controls with no horizontal overflow, invalid Settings backup rejection without state loss, Settings backup restore of Community posts and Messages threads, and Settings backup export/import/reset flows.

## Recommended Next Steps

1. Run a 3 to 10 person private beta through registration, sync, problem practice, mock interviews, and resume flows.
2. Watch Render logs, Resend delivery, and OpenAI spend during the beta.
3. Configure a real external alert receiver and edge-level rate limits, then make `npm run check:ops-alerts:production` and `npm run check:ops-alerts:production -- --smoke` pass with provider/notes/HTTPS DNS-hostname evidence URL signoff plus real receiver delivery, signature verification, and verification acknowledgement; keep `npm run check:ops-alerts:runtime-smoke`, `npm run check:ops-alerts:production-fixture`, and `npm run build:ops-alert-edge-packet` in local release checks for API webhook delivery, auth rate-limit enforcement, production-signoff regression coverage including raw-IP/credential/query URL rejection, smoke payload safety/signing/acknowledgement, and deployment handoff artifacts, and extend the small admin UI if beta operators need deeper drilldowns.
4. Keep large/private local artifacts out of Git with `npm run check:repo-hygiene`; only normalized catalogs and generated runtime assets should remain tracked.
5. Plan and execute the public-release database cutover to Postgres using `api-server/postgres/schema.sql`, `npm run check:postgres-cutover`, `npm run check:postgres-cutover:export-smoke`, `npm run build:postgres-cutover-packet`, `npm run check:postgres-cutover:complete`, and a protected, non-truncated `--include-sensitive` SQLite export as the migration input. The local checks validate the export, an offline import plan, migration-window handoff files, and final signoff evidence positive/negative shape including private-network/raw-IP target rejection, private/raw-IP/credential/query evidence URL rejection, source/export hash binding, target row-count binding, app-DB-active confirmation, runtime backend/health URL evidence, and backup confirmation, plus target/database/evidence URL hardening; the final cutover still needs a real managed Postgres target and protected migration window.
6. Configure a real S3/R2 media bucket and CDN/public base URL for production uploads, then make `npm run check:media-storage:production` and `npm run check:media-storage:production -- --live` pass against those environment variables; keep `npm run check:media-storage:runtime-smoke`, `npm run check:media-storage:production-fixture`, and `npm run build:media-storage-packet` in local release checks for local disk, S3/R2-compatible behavior, production-shape rejection including raw-IP/credential/query/raw-provider URL hardening, bucket/prefix hardening, live-smoke cleanup plus Content-Type and Range coverage, and deployment handoff artifacts.
7. Keep `npm run check:jobs-api:deployed-source` passing against `https://api.quantgym.app/api/jobs` after public-ATS feed refreshes and API deploys; keep `npm run check:jobs-source:runtime-smoke`, `npm run check:jobs-source:production-fixture`, `npm run check:jobs-feed:static`, `npm run check:jobs-source:production -- --live`, and `npm run build:jobs-feed:publication-packet` in local release checks for cache, merge, filtering, bad-feed fallback behavior, production source URL/token hardening including raw-IP URL rejection, static feed hosting evidence, live-feed quality coverage, and feed hosting handoff artifacts.
8. Submit the Chrome Collector release package through the Chrome Web Store developer dashboard and wait for review approval after the local runtime, hashed store-readiness, publication handoff, publication fixture, and `npm run build:chrome-store-publication-packet` gates pass; then run `npm run check:chrome-store-publication:published` with HTTPS DNS-hostname published listing/evidence URLs for the same Chrome Web Store item id, manifest-matching submitted version, and current release package SHA-256.
9. Before any public or commercial question-bank distribution, refresh `npm run build:question-bank-rights-packet`, collect per-source approval evidence with externally reachable HTTPS DNS evidence URLs into `data/question-banks/source-rights-manifest.json`, and make `npm run check:question-bank-rights:public`, `npm run check:question-bank-rights:commercial`, `npm run check:question-bank-rights:public-smoke`, and `npm run check:question-bank-rights:release-blockers` pass so the launch-blocker rollup can clear the rights item with current packet evidence.
10. Keep expanding the browser route smoke into broader browser-executed user journeys, and keep the module ownership manifest current whenever route/page ownership changes.
